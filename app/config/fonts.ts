// Configuración de fuentes para la aplicación
// Este archivo separa la configuración de fuentes para evitar
// conflictos con Babel en la compilación

import { Inter } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
}) 